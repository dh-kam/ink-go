schemaVersion: goink.tuitest/v1alpha1
name: use-stderr-demo/two-writes
app: use-stderr-demo
viewport:
  width: 100
  height: 30
environment:
  TERM: tmux-256color
  COLORTERM: ""
  FORCE_COLOR: "1"
capture:
  mode: screen
steps:
  - name: two-writes
    waitFor:
      text: |-
        Hello from Ink to stderr
        Hello from Ink to stderr
      within: 6s
    expect:
      file: use-stderr-two-writes.screen.golden
  - name: ctrl-c-exit
    input:
      key: ctrl-c
    exit:
      within: 5s
    expect:
      sameAsPrevious: true
